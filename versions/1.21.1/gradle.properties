minecraft_version=1.21.1
# VulkanMod 0.6.7 (deps.vulkanmod below) requires Fabric Loader >=0.18.0.
loader_version=0.18.4
fabric_version=0.116.6+1.21.1
cloth_config_version=15.0.140
modmenu_version=11.0.3
loom_version=1.16-SNAPSHOT
deps.sodium=mc1.21.1-0.6.13-fabric
deps.sodium_maven=modrinth
# Sodium also ships 0.8.x betas for 1.21.1 (its own concurrent release branch, not tied to a newer
# MC version). deps.sodium above stays pinned to the stable 0.6.x line for dev/runtime; this second,
# compile-only-only pin exposes the 0.8+ sodium:config_api_user API classes so GreedySodiumConfigEntryPoint
# compiles too, letting the same jar serve options through whichever Sodium branch the end user runs.
deps.sodium_new_api=mc1.21.1-0.8.12-fabric
deps.iris=1.8.8+1.21.1-fabric
deps.grounded=1.21.1-0.2.0
deps.moonlight=1.21-2.29.32-fabric
deps.java_version=21
deps.use_mojmap=true
deps.unobfuscated=false
# VulkanMod support (opt-in, 0.6.x API). Modrinth version ID for this MC version.
deps.vulkanmod=2eqroDKD
